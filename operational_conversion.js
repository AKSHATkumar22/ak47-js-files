let score = 33;

const scoreNumber = Number(score);

console.log(scoreNumber);

// score Number converion to Number prints a integer value

let Score = "33";

const ScoreNumber = String(Score);

console.log(ScoreNumber);

// means what happens is that when we try to write a number in a
// string manner and tries to print the number in a string then is does not print
// gives it a string value

let ak47LoggedIn = 1;

const Login = Boolean(ak47LoggedIn);

console.log(Login);

// what happens is that

// when we try to write a number in the format of

// boolean 1 - gives it a  --> true value
// boolean 0 - gives it a --> false value
